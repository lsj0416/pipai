# 백엔드 API 서버 주소 (브라우저에서도 접근)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# JWT 시크릿 (서버사이드 미들웨어 전용 — NEXT_PUBLIC 사용 금지)
JWT_SECRET=change-me-in-production

# Next.js 앱 URL
NEXTAUTH_URL=http://localhost:3000
